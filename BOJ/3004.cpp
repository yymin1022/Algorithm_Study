#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0);
    cout.tie(0);
    ios_base::sync_with_stdio(false);

    int N;
    cin >> N;

    int row = N / 2 + 1;
    int col = N - row + 2;
    coug << row * col << "\n";

    return 0;
}